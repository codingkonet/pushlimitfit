{
  "name": "pushlimitfit",
  "version": "2.0.0",
  "private": true,
  "description": "Offline-first fitness platform — all data stored in the browser via localStorage. No backend, no accounts.",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build"
  }
}
